name: cgm_monitor
description: CGM Monitor — xDrip companion app with NFC & Bluetooth support.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  provider: ^6.1.2
  fl_chart: ^0.70.2
  nfc_manager: ^3.5.0
  flutter_blue_plus: ^1.35.2
  shared_preferences: ^2.5.3
  http: ^1.4.0
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
